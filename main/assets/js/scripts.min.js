const carousel=document.querySelector(".carousel"),slider=document.querySelector(".slider");let direction=-1;const numSlides=slider.childElementCount;let translateAmount=`${100/numSlides}%`;carousel.style.cssText=`
                         width:100%;
                         height: 100%;
                         display: grid;
                         grid-template: 1fr / 100%;
                         position: relative;
                         justify-items = start;
                         overflow: hidden;
                        `,slider.style.cssText=`
                      height: 100%;
                      width: ${100*numSlides}%;
                      display: grid;
                      grid-template-columns: repeat(${numSlides}, ${1/numSlides}fr);
                      transition: all 0.5s;
                      `,slider.addEventListener("click",e=>{var t=e.target.classList.contains("next"),e=e.target.classList.contains("prev");t?(1===direction&&(slider.prepend(slider.lastElementChild),carousel.style.justifyItems="start"),direction=-1,slider.style.transform=`translateX(-${translateAmount})`):e&&(-1===direction&&(direction=1,slider.appendChild(slider.firstElementChild),carousel.style.justifyItems="end"),slider.style.transform=`translateX(${translateAmount})`)}),slider.addEventListener("transitionend",function(){1===direction?slider.prepend(slider.lastElementChild):(console.log("about to add first child"),slider.appendChild(slider.firstElementChild)),slider.style.transition="none",slider.style.transform="translate(0)",setTimeout(function(){slider.style.transition="all 0.5s"})},!1);const menuIcons=document.querySelector(".menu-icons");menuIcons.addEventListener("click",()=>toggleClasses());const toggleClasses=()=>{document.querySelector(".header__nav").classList.toggle("open"),document.getElementById("hamburger").classList.toggle("is-hidden"),document.getElementById("cross").classList.toggle("is-hidden")};